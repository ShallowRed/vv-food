name: foude

services:
  foude:
    build:
      context: .
    container_name: foude
    environment:
      PORT: 3000
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped